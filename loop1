- name: looping
  hosts: local
  connection: local
  tasks:
  - name: printing the loops
    ansible.builtin.package:
     name: "{{ item.name }}"
     state: "{{ item.state }}"
    loop:
    - {name: 'mysql', state: 'present'}
    - {name: 'gcc', state: absent'}